# Tyagach — план улучшений (backlog)

Обновлён: 2026-07-09. Контекст и обоснования: `TYAGACH_STRATEGY_REVIEW_2026-07-09.md`.
P1 (вето входов 12–15h UTC) и P2 (фил-гвард ≥70% BS-mid) — ВНЕДРЕНЫ 2026-07-09.
P3 (калибровка на 85% fill) — правило анализа, см. ARCHITECTURE.md Amendment 2026-07-09.

## P4 — Ops-долги (сделать до перевода Tyagach на реальные деньги)

- [ ] **Бэкапы tyagach.db** — сейчас НЕТ НИ ОДНОГО. SQLite живёт в docker-томе
      (`/app/data/tyagach.db`). Нужен cron: ежедневный дамп (`sqlite3 .backup`)
      в /root/backups с ротацией 14д + периодическая scp-копия на Mac.
      Делать по образцу задачи для opt-app Postgres (BEFORE_BOBA_LIVE.md §3).
- [ ] **Klines-кэш loop↔api** — оба контейнера независимо тянут ETHUSDT klines
      с Bybit и ловят rate-limit 10006 ежечасно (pybit ретраит, потерь нет, но
      это трение и лишний вектор сбоя). Фикс: loop пишет klines в SQLite,
      api читает оттуда. (TODO от 07-04.)
- [ ] **Runtime-редактирование CELL_CONFIG** — сейчас любое изменение параметров
      ячейки = пересборка контейнера. Вынести в env/БД + endpoint (отложено 07-08).
- [ ] **Санити-алерт на «тихий» loop** — если > N часов нет ни одного цикла
      обработки TF (курсоры не двигаются), слать Telegram. Сейчас залипание
      увидим только глазами.

## P5 — Исследовательский бэклог (по мере накопления live-данных)

- [ ] **Пересмотр override 15m/OB и 1h/OB** — оба НЕ проходят all-3-splits в
      4y-свипе, включены 07-07 волевым решением по live-выборке. Правило:
      пересмотреть после 20–30 живых OB-закрытий на ячейку.
- [ ] **BB per-cell depth sweep** — BB торгуется на дефолтах (0.5/3.0/0.5, iv≥55);
      depth-оптимизация как у OB не делалась. Делать, когда у BB будет
      осмысленная живая выборка (сейчас 1 сделка).
- [ ] **MB re-sweep с per-cell параметрами** — ОТОДВИНУТ решением юзера 07-09
      (live-убытки −$33.7/15). Прайор у MB лучше, чем у младших TF (проходил
      all-3-splits до depth-оптимизации; убит на выборке из 15 сделок), но
      возвращаться только после того, как OB-ядро стабильно профитит live.
- [ ] **DOW-эффекты** (Сб +$3.20/сделку, Чт −$0.06) — слабее часового эффекта,
      риск оверфита; смотреть только вместе с пересмотром override'ов.
- [ ] **BTC-версия Tyagach** — новая ветка, далеко от live; не начинать, пока
      ETH-ядро не вышло на реальные деньги.

## Закрыто / не пересматривать без новых данных

- 5m и 10m TF — REJECTED 2026-07-09 (fee-adjusted sweep: 5m 0/816 даже на
  holdout-отдельно; 10m валится на validation при любых параметрах, r_target до 15).
- Global same-direction блок — per-TF стакинг выигрывает все сплиты (A/B 07-05).
- Sizing grid-search — «оптимум» = артефакт некэпированного компаундинга (07-03).
- Exit-механика поверх r_target — r_target свипнут; exit-рычаги в sibling-проектах
  отвергнуты многократно.
