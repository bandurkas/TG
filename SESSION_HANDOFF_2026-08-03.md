# Tyagach: сессия 2026-08-03 — итог, продолжать в новом чате отсюда

> Всё описанное ниже уже задеплоено и подтверждено живьём (VPS3 на коммите
> `ec00ae5`). Раздел "Следующие шаги" внизу — куда двигаться дальше.

## Что сделано за сессию (хронологически)

### 1. SL-буфер, раунд 1 — ATR(14) для 1h/OB + 1h/FVG (коммиты `ff5b19b`, `f3ea390`)
Продолжение прошлой сессии (`SL_BUFFER_HANDOFF_2026-08-02.md`): флэт
`BUFFER_FRAC=0.0015` не масштабировался по TF/волатильности. Прогнал
`src/sl_buffer_atr_sweep.py` (нашёл и починил баг — `IndexError` на
последней свече датасета). Результат: ATR-буфер (`mult=2.0`) помогает
**только** 1h/OB и 1h/FVG (holdout calmar 1.50→2.53, 5.61→9.20), на
15m/30m портит из-за корреляции убытков в трендовых сериях (реплей на
реальных 08-02 SL-сделках это подтвердил количественно). Новый
`config.ATR_BUFFER_MULT` dict, scope — только эти 2 ячейки.

### 2. SL-буфер, раунд 2 — flat-регрид для 7 ещё ячеек (коммит `35bf0b7`)
Пользователь попросил не считать flat "меньшим злом", а реально
переретюнить. Тонкая сетка (flat 0.0015-0.006 + capped-ATR) по всем 9
ячейкам 15m/30m/2h × OB/FVG/MB нашла: **не ATR, а просто чуть шире
константа** — новый `config.FLAT_BUFFER_FRAC_OVERRIDE` dict:
`15m/FVG=0.0025, 15m/MB=0.005, 30m/OB=0.002, 30m/FVG=0.003, 30m/MB=0.0025,
2h/OB=0.005, 2h/FVG=0.004`. 15m/OB и 2h/MB — без изменений (ничего не
побило текущий). Portfolio combo-check: validation calmar 21.8→35.7,
holdout 25.6→27.3, 0/8 отрицательных кварталов сохранено.

Честная находка при реплее на реальных сделках: ни один из новых буферов
(ни ATR, ни расширенный flat) не спасает конкретно серию 02.08 — 02.08
был достаточно сильным трендом, чтобы пробить любой разумный буфер.
Выигрыш статистический за 4 года, не «фикс» той серии.

### 3. Manual close-all / close-one для Tyagach (коммит `507b54d`)
**Баг**: кнопка "Закрыть все" в Mission Control только паузила бота и
инвалидировала pending zone-сигналы — реальные открытые позиции не
закрывала (сам `api.py` признавался в этом в докстринге, фронтенд честно
предупреждал). Портировал уже проверенный паттерн Jony 1-в-1: API кладёт
запрос в БД (`close_all_requested` флаг / `close_requests` таблица), loop
(единственный writer) исполняет реальный выкуп на следующем тике через
тот же `_execute_close`, что и обычный SL/TP — новой логики исполнения
ордеров не писал, только проводка существующего пути по требованию.
Плюс closeone-position по запросу пользователя. Race-safe (проверено
тестами): `close_position_now` перечитывает `get_open_position`
непосредственно перед исполнением, гонка с SL/TP-свипом в тот же тик —
тихий no-op, не краш и не двойное закрытие.

### 4. Frontend: live PnL + кнопки (репо `bandurkas/opt`, коммит `16c73e60`)
`unrealized_pnl_usd` уже считался бэкендом (и у Tyagach, и у Jony), просто
не отображался. Добавлен PnL-бейдж (зелёный/красный) на каждую открытую
позицию у **обоих** ботов + кнопка close-one для Tyagach (у Jony уже
была). Обновлён текст предупреждения в MissionControl.tsx (больше не
"НЕ закрывает реальные позиции" — теперь правда закрывает). Vercel
задеплоил автоматически по пушу в main.

### 5. Исследование: trailing take-profit по всей корзине — ОТРИЦАТЕЛЬНЫЙ результат, не внедрено
Построил отдельный симулятор с непрерывным mark-to-market
(`src/basket_trailing_stop_research.py`) — все прошлые скрипты считали
только вход/один финальный выход, без промежуточного пути, для этого
вопроса такого было недостаточно. Self-check: воспроизвёл известные
метрики baseline почти точно (доверяю механике). Свип по arm-порогу
(0.5-2% от баланса) × trail-проценту (30-70%) на всех 3 сплитах: **чем
агрессивнее trailing, тем хуже calmar** — обрезает победителей до
полного R-multiple тейка, просадка снижается на 1-2пп, но return падает
гораздо сильнее. При редком срабатывании (arm=2%) результат просто
сходится к baseline. **Вывод: не внедрять.** Скрипт закоммичен как
документированное исследование, в проде не используется.

### 6. TP-ретюн (4 ячейки) + деактивация 15m/BB (коммит `ec00ae5`)
`src/tp_retarget_sweep.py` — свип r_target по всем 13 живым ячейкам (SL
уже финальный из п.1-2). Важная находка: fee_drag **не зависит** от
r_target (комиссия capped на 12.5% от премии, не за сделку) — опасение
"чаще брать профит = переплата за комиссии" не подтвердилось.

Изменения в `CELL_CONFIG`:
| ячейка | было | стало |
|---|---|---|
| 15m/MB | r_target=7.0 | **2.1** |
| 30m/OB | r_target=10.0 | **5.0** |
| 30m/FVG | r_target=7.0 | **3.5** |
| 2h/OB | r_target=5.0 | **3.75** |

Побочная находка: 15m/BB на полных текущих данных при живом
`expiry_days=5.0` — calmar ровно **−1.00** (монотонный слив), 8/8
отрицательных кварталов, maxDD 62-64%. Доследовал sweep по expiry_days
(0.083-5.0 дней, монотонная деградация с ростом холдинга) — даже лучший
альтернативный вариант (3ч, уже тестировался и отвергался месяцы назад)
всё ещё слабый (holdout calmar −0.24, 2/8 отрицательных). Структурная
хрупкость (меньше всего зон среди всех kind), не проблема тюнинга.
**15m/BB убран из `ACTIVE_CELLS`** (открытых BB-позиций на момент деплоя
не было).

Комбинированный portfolio combo-check (`tp_and_bb_deactivation_combo_check.py`):
validation calmar 35.7→**106.2**, holdout 27.3→**68.1**, holdout maxDD
19.8%→**14.9%** (доходность и риск улучшились одновременно), 0/8
отрицательных кварталов сохранено.

## Текущее живое состояние (VPS3, коммит `ec00ae5`)

- **ACTIVE_CELLS** (12 ячеек, BB выключен): 15m/OB, 15m/MB, 15m/FVG,
  30m/OB, 30m/MB, 30m/FVG, 1h/OB, 1h/MB, 1h/FVG, 2h/OB, 2h/MB, 2h/FVG.
- **SL-буфер**: 1h/OB и 1h/FVG на ATR(14)×2.0; 7 ячеек на расширенном
  flat (см. п.2); остальные на дефолтном flat 0.0015.
- **TP (r_target)**: 4 ячейки ретюнены (см. таблицу п.6), остальные без
  изменений.
- Оба контейнера (`tyagach_loop`, `tyagach_api`) живы, здоровы.
- **Бот на паузе** (`paused=1` в bot_state, подтверждено через `/state`
  прямо перед записью этого файла) — это НЕ связано с сегодняшними
  изменениями, состояние было такое ещё до начала сессии (сохраняется в
  БД, переживает рестарты контейнеров). Стоит спросить пользователя,
  почему стоит на паузе и не забыть возобновить, если это не осознанное
  решение.
- **1 открытая позиция** прямо сейчас: id=36, 1h/FVG bullish, PUT
  ETH-3AUG26-1875-P, вход @1872.60, unrealized_pnl≈−$3.54 (на момент
  записи файла). Управляется независимо от паузы (SL/TP/expiry свипы не
  гейтятся паузой) — новый close-all/close-one теперь тоже реально её
  закроет при необходимости.
- Manual close-all/close-one работают по-настоящему (не только сигналы).
- Frontend (Vercel, репо `bandurkas/opt`) показывает live PnL на каждой
  открытой позиции у Tyagach и Jony.

## Где что лежит

- **Репо**: `~/Desktop/Tyagach` (root, GitHub `bandurkas/TG`, branch
  `main`), приложение в `tyagach/`, research в `src/`, данные в `data/`.
- **VPS3**: `187.127.114.34`, `/root/tyagach/tyagach`, креды в памяти
  `reference_vps3.md`. Деплой: `git fetch && git merge origin/main
  --no-edit && docker compose build && docker compose up -d
  --force-recreate` (запускать из `/root/tyagach/tyagach`).
- **Frontend**: `~/Desktop/options/frontend` (репо `bandurkas/opt`,
  branch `main`), Vercel auto-deploy по пушу.
- **Живая копия БД** (снапшот на момент сессии, 07:58 UTC):
  `~/Desktop/Tyagach/db_backups/tyagach_live_20260803_0758.db` — уже
  устарела после деплоев, тянуть свежую через `docker compose cp
  tyagach_api:/app/data/tyagach.db` на VPS3, если нужна для анализа.
- Новые research-скрипты за сессию (все в `src/`, все закоммичены):
  `sl_buffer_atr_sweep.py`, `sl_buffer_atr_1h_combo_check.py`,
  `sl_buffer_atr_retune_15m_30m_2h.py`, `sl_buffer_15m_retune_v2.py`,
  `sl_buffer_full_retune_combo_check.py`, `basket_trailing_stop_research.py`,
  `tp_retarget_sweep.py`, `tp_and_bb_deactivation_combo_check.py`.

## Следующие шаги (варианты, ничего не решено)

1. **Спросить пользователя**, почему бот на паузе, и возобновить, если
   да — иначе все сегодняшние улучшения просто не торгуют.
2. Понаблюдать за живыми метриками 1h/15m/30m/2h-ячеек несколько дней —
   особенно 15m/MB и 30m/OB (самые агрессивные TP-изменения) и убедиться,
   что реальные сделки не расходятся с бэктестом.
3. 2h/MB TP-ретюн был неоднозначным (rt=1.5 улучшает holdout, портит
   validation) — не трогали, можно вернуться при желании.
4. Если захочется всё же посмотреть trailing-профит ещё раз — можно
   попробовать другую конструкцию (например, защита только после очень
   большого пика), но текущее исследование не даёт оснований усложнять.
5. Полный цикл (research→code→review→tests→deploy) — установившийся за
   сессию паттерн, продолжать в том же духе для будущих изменений.
